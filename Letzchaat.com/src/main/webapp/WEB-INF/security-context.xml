<?xml version="1.0" encoding="UTF-8"?>
<beans:bean xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security.xsd">
 
 <http auto-config="true">
	 	<intercept-url pattern="/" access="permitAll" />
		<intercept-url pattern="/index" access="permitAll" />
	 <intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
	 <intercept-url pattern="/user/**" access="hasRole('ROLE_USER')" />
		<form-login login-page="/login" username-parameter="emailId" password-parameter="password" login-processing-url="/ulogin"
			authentication-failure-url="/login?error" />
			
		<logout logout-url="/logout" delete-cookies="true"
			logout-success-url="/login?logout" />
		<csrf disabled="true" />
</http>

	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="myDataSource"
				users-by-username-query="select emailid,password,isActive from user where emailid=?"
				authorities-by-username-query="select u1.emailid , u2.role from user u1, Authorization u2 where u1.emailid=u2.emailid and u1.emailid=?" />
		</authentication-provider>
	</authentication-manager>
</beans:bean>